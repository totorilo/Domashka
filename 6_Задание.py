import math
R = input("переменная R")

print("перменная " + str(R))

h = input("переменная h")

print("перменная " + str(h))

M = float(float(R)**2 + float(h)**2)

L = float(float(M**0.5))

Y= round(float(L), 5)

print("Ответ " + str(Y))
